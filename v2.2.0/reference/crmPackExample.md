# Open the Example PDF for crmPack

**\[stable\]**

Calling this helper function opens the `example.pdf` document, residing
in the `doc` subfolder of the package installation directory.

## Usage

``` r
crmPackExample()
```

## Value

Called for side effects.
