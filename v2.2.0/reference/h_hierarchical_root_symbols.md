# Find the Root Symbol of an Expression

Find the Root Symbol of an Expression

## Usage

``` r
h_hierarchical_root_symbols(expr)
```

## Arguments

- expr:

  (`language`)\
  expression to inspect.

## Value

Character vector of root symbols.
