# Format Dose-Axis Labels

Formats dose values without scientific notation or trailing zeroes so
the same labels can be used across dose-related plots.

## Usage

``` r
h_dose_axis_labels(x)
```

## Arguments

- x:

  (`numeric`)\
  dose values.

## Value

A character vector of formatted dose labels.
