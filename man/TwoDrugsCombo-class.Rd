% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TwoDrugsCombo-class.R
\docType{class}
\name{TwoDrugsCombo-class}
\alias{TwoDrugsCombo-class}
\alias{.TwoDrugsCombo}
\alias{TwoDrugsCombo}
\alias{.DefaultTwoDrugsCombo}
\title{\code{TwoDrugsCombo}}
\usage{
TwoDrugsCombo(single_models, gamma = 0, tau = 1, log_normal_eta = FALSE)

.DefaultTwoDrugsCombo()
}
\arguments{
\item{single_models}{(\code{list})
named list of length 2 with compatible single-agent \code{\link{GeneralModel}}
objects, one per drug.}

\item{gamma}{(\code{number})
prior mean parameter for the interaction term.}

\item{tau}{(\code{number})
prior precision parameter for the interaction term.}

\item{log_normal_eta}{(\code{flag})
should the interaction term use a log-normal prior?}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{\link{TwoDrugsCombo}} is the class for a two-drug combination regression model with
fixed priors for the two single-agent dose-toxicity models and an additional
interaction parameter.
}
\details{
Let \eqn{p(x_1, x_2)} be the probability of DLT at the dose
combination \eqn{(x_1, x_2)}. The model combines two single-agent models
with an interaction term:
\deqn{\textrm{odds}(p(x_1, x_2)) = \textrm{odds}(p_0(x_1, x_2)) *
  \exp\left(\eta * I(x_1, x_2)\right),}
where \eqn{p_0(x_1, x_2) = 1 - (1 - p_1(x_1))(1 - p_2(x_2))} and each
single-agent probability follows a model \eqn{p_j(x_j)}. The normalized dose
\eqn{\tilde{x}_j} is extracted from the single-agent model's dose covariate,
e.g. \eqn{x_j / x_j^{*}}, \eqn{x_j - x_j^{*}}, or \eqn{x_j}.
The interaction parameter \eqn{\eta} has either a normal prior or, if
\code{log_normal_eta = TRUE}, a log-normal prior.
}
\section{Slots}{

\describe{
\item{\code{single_models}}{(\code{list})
named list of length 2 containing single-agent \code{\link{GeneralModel}} objects,
one per drug. Each model must use \code{nObs}, \code{y}, and \code{x} as data inputs and
contain a Bernoulli likelihood for \code{y} in its \code{datamodel}.}

\item{\code{ref_dose}}{(\code{numeric})
optional reference doses extracted from \code{single_models}, if provided.}

\item{\code{drug_names}}{(\code{character})
the names of the two drugs.}

\item{\code{gamma}}{(\code{numeric})
prior mean parameter for the interaction term.}

\item{\code{tau}}{(\code{numeric})
prior precision parameter for the interaction term.}

\item{\code{log_normal_eta}}{(\code{flag})
should the interaction term use a log-normal prior?}
}}

\note{
Typically, end users will not use the \code{.DefaultTwoDrugsCombo()} function.
}
\examples{
my_model <- TwoDrugsCombo(
  single_models = list(
    drug1 = LogisticLogNormal(
      mean = c(-0.85, 1),
      cov = matrix(c(1, -0.5, -0.5, 1), nrow = 2),
      ref_dose = 10
    ),
    drug2 = LogisticLogNormal(
      mean = c(-0.7, 0.8),
      cov = matrix(c(1.1, -0.3, -0.3, 0.9), nrow = 2),
      ref_dose = 20
    )
  ),
  gamma = 0,
  tau = 1
)

my_model
}
\seealso{
\code{\link{LogisticLogNormal}}, \code{\link{DataCombo}}.
}
