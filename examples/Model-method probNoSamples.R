## obtain the probability of the occurrence of a DLE for a given dose level using a DLE model without samples
## DLE model can be models from 'ModelTox' class (model slot)
## Given dose level 50 mg (dose slot), to obtain the probability of the occurrence of DLE at this dose level
prob(dose=50, model=model)