# Deparse a Model Body into JAGS Lines

Deparse a Model Body into JAGS Lines

## Usage

``` r
h_hierarchical_model_body_lines(model_fun)
```

## Value

Character vector containing the body expressions without braces.
