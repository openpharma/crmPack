# Collect Prior Relations

Collect Prior Relations

## Usage

``` r
h_hierarchical_prior_relations(expr, operator)
```

## Arguments

- expr:

  (`language`)\
  prior model body.

- operator:

  (`string`)\
  relation operator, either `"~"` or `"<-"`.

## Value

A list of relation calls.
